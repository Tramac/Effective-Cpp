#include "head.h"

int main()
{
    // -----Demo 1.-----
    Rational a(1, 2);
    Rational b(3, 5);

    // Rational c = a * b;
    
    Rational w, x, y, z;
    w = x * y * z;

    if ((x * y) == (z * w)) {
    }

    return 0;
}
